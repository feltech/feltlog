/**
 * Entry editor route.
 *
 * Re-exports the shared JournalEntryEditorScreen component so it can be navigated to as
 * a modal route via Expo Router's file-based routing.
 */
export { default } from '@/src/presentation/components/JournalEntryEditorScreen';
