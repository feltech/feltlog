/**
 * Database setup route.
 *
 * Re-exports the shared SetupDatabaseScreen component so it can be navigated to as a
 * first-class Expo Router route inside the root Stack navigator.
 */
export { default } from '@/src/presentation/components/SetupDatabaseScreen';
