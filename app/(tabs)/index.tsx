/**
 * Journal list route.
 *
 * Re-exports the shared JournalScreen component so it can be navigated to as the
 * default tab route via Expo Router's file-based routing.
 */
export { default } from '@/src/presentation/components/JournalScreen';
