/**
 * Lock screen route.
 *
 * Re-exports the shared LockScreen component so it can be navigated to as a first-class
 * Expo Router route inside the tab navigator.
 */
export { default } from '@/src/presentation/components/LockScreen';
