/**
 * Tab navigation layout route.
 *
 * Re-exports the shared TabLayout component so Expo Router can use it as the layout for
 * the (tabs) route group.
 */
export { default } from '@/src/presentation/components/TabLayout';
