appId: com.feltech.feltlog
name: Restore from backup — after cancel


# E2E test for the Restore from Backup feature.
# Verifies the entry point on the setup screen lets the user restore
# a database from a backup file.
#
# Flow B: Cancel/return flow + restore with location reconfiguration.
#   Tests that Cancel returns to setup, that the backup location can be
#   reconfigured on the restore screen, and that restore succeeds.
#
# Note: The overwrite confirm dialog (safety backup for existing DB)
# cannot be tested via clearState because `adb pm clear` wipes all app
# data including DB files. After clearState, there's never a DB at the
# target name to trigger the overwrite path. That path is covered by
# unit tests in useRestoreFlow.test.ts.
---
# Flow B: Cancel/return flow + restore with location reconfiguration.
# Tests that Cancel returns to the setup screen, that the backup location
# can be configured on the restore screen, and that restore succeeds.
- clearState
- launchApp

# Wait for the app to fully load after clearState kills the process.
# Use optional: true so we can retry if the app lands on the home screen.
- extendedWaitUntil:
    visible:
      id: 'db-name-input'
    timeout: 15000
    optional: true

# If the setup screen didn't appear, relaunch the app
- runFlow:
    when:
      notVisible:
        id: 'db-name-input'
    commands:
      - launchApp
      - extendedWaitUntil:
          visible:
            id: 'db-name-input'
          timeout: 30000

# ===== INITIAL SETUP =====
- evalScript: ${output.dbName = "restore_cancel_" + Date.now() + ".db"}
- evalScript: ${output.dbKey = "restore_key_cancel"}

- assertVisible:
    id: 'db-name-input'
- tapOn:
    id: 'db-name-input'
    point: '95%,50%'
- eraseText
- inputText: ${output.dbName}
- tapOn:
    id: 'db-key-input'
- inputText: ${output.dbKey}
- tapOn:
    id: 'db-open-btn'

- assertVisible: 'Create entry'

# ===== CREATE AN ENTRY AND BACKUP =====

# Dismiss LogBox warning if present — it can cover the FAB
- runFlow:
    when:
      visible: 'Open debugger to view warnings.'
    commands:
      - tapOn:
          point: '93%,92%'
      - waitForAnimationToEnd

- setLocation:
    latitude: 53.787977855061214
    longitude: -2.244492876694697

- evalScript: ${output.entryText = "Restore cancel test " + Date.now()}
- tapOn:
    id: 'create-entry-fab'
- assertVisible:
    id: 'entry-content-input'
- tapOn:
    id: 'entry-content-input'
- inputText: ${output.entryText}
# Save entry and navigate back by tapping the back button directly.
# The entry editor's beforeRemove listener intercepts back navigation,
# calls e.preventDefault(), and does an async save (createEntry +
# loadEntries + loadTags). On slow emulators, this chain can take
# variable time. If the first tap doesn't complete the navigation
# within 10 seconds, the isLeavingRef guard allows a second back
# press to bypass the handler and navigate immediately.
- tapOn: 'Back'
- extendedWaitUntil:
    visible:
      id: 'create-entry-fab'
    timeout: 10000
    optional: true
# If still on the entry editor, press BACK again — the beforeRemove
# guard (isLeavingRef.current = true) lets the second press through.
- runFlow:
    when:
      visible: 'Back'
    commands:
      - tapOn: 'Back'
- extendedWaitUntil:
    visible:
      id: 'create-entry-fab'
    timeout: 60000
- assertVisible:
    id: 'create-entry-fab'
- takeScreenshot: restore-flow-b-entry-created

# ===== NAVIGATE TO SETTINGS AND CONFIGURE BACKUP =====

- waitForAnimationToEnd
- tapOn:
    point: '75%,96%'
- waitForAnimationToEnd
- takeScreenshot: restore-flow-b-settings

- assertVisible: 'Backup'
- assertVisible: 'No backup location configured'

# Open SAF picker for backup location
- tapOn:
    id: 'choose-backup-location-btn'
- waitForAnimationToEnd

- runFlow:
    when:
      visible: 'sdk_gphone64_x86_64'
    commands:
      - tapOn: 'sdk_gphone64_x86_64'
      - waitForAnimationToEnd

- scrollUntilVisible:
    element: 'Pictures'
    direction: DOWN
    timeout: 10000
- runFlow:
    when:
      visible: 'Pictures'
    commands:
      - tapOn: 'Pictures'
      - waitForAnimationToEnd

- tapOn: 'USE THIS FOLDER'
- waitForAnimationToEnd
- tapOn: 'ALLOW'
- waitForAnimationToEnd

# Dismiss LogBox warning banner if present
- runFlow:
    when:
      visible: 'Open debugger to view warnings.'
    commands:
      - tapOn:
          point: '93%,92%'
      - waitForAnimationToEnd

- assertVisible: 'Location: (Pictures|Documents)'

# Guard against minute-boundary races
- evalScript: ${output.backupSecond = new Date().getUTCSeconds()}
- runFlow:
    when:
      true: ${output.backupSecond >= 55}
    commands:
      - extendedWaitUntil:
          visible:
            id: 'wait-element-for-minute-crossing'
          timeout: 6000
          optional: true

- runScript: scripts/compute-backup-prefix.js

- tapOn:
    id: 'backup-now-btn'
- waitForAnimationToEnd

- extendedWaitUntil:
    visible: 'Backup saved'
    timeout: 60000
- assertVisible: 'Backup saved'
- takeScreenshot: restore-flow-b-backup-done

# ===== WIPE STATE AND TEST CANCEL/RETURN FLOW =====

- clearState
- launchApp

# Wait for the app to fully load after clearState kills the process.
# Use optional: true so we can retry if the app lands on the home screen.
- extendedWaitUntil:
    visible:
      id: 'db-name-input'
    timeout: 15000
    optional: true

# If the setup screen didn't appear, relaunch the app
- runFlow:
    when:
      notVisible:
        id: 'db-name-input'
    commands:
      - launchApp
      - extendedWaitUntil:
          visible:
            id: 'db-name-input'
          timeout: 30000

# If still not visible after relaunch, try once more (emulator may be slow)
- runFlow:
    when:
      notVisible:
        id: 'db-name-input'
    commands:
      - launchApp
      - extendedWaitUntil:
          visible:
            id: 'db-name-input'
          timeout: 60000

- runFlow:
    when:
      visible: 'Open debugger to view warnings.'
    commands:
      - tapOn:
          point: '93%,92%'
      - waitForAnimationToEnd

- assertVisible:
    id: 'db-name-input'
- assertVisible:
    id: 'restore-backup-btn'

# Tap "Restore from backup" to open the restore screen
- tapOn:
    id: 'restore-backup-btn'
- waitForAnimationToEnd
- takeScreenshot: restore-flow-b-restore-screen

# Verify restore screen elements.
# Note: the encryption key input was removed — restore is now a pure
# file copy that returns the user to the setup screen.
- assertVisible:
    id: 'restore-db-name-input'
- assertVisible:
    id: 'restore-confirm-btn'
- assertVisible:
    id: 'restore-cancel-btn'

# Tap Cancel — should return to the setup screen
- tapOn:
    id: 'restore-cancel-btn'
- waitForAnimationToEnd
- takeScreenshot: restore-flow-b-back-to-setup

# Verify we're back on the setup screen
- assertVisible:
    id: 'db-name-input'
- assertVisible:
    id: 'db-open-btn'
- assertVisible:
    id: 'restore-backup-btn'

# ===== NOW TEST THE FULL RESTORE FLOW =====

# Tap "Restore from backup" again
- tapOn:
    id: 'restore-backup-btn'
- waitForAnimationToEnd

# Verify the "No backup location configured" error is shown on the
# RestoreFromBackupScreen. The full text is "No backup location
# configured. Tap "Choose Backup Location" below." — use regex for
# a partial match since Maestro matches full element text.
# (clearState wiped AsyncStorage which held the backup dir URI)
- assertVisible: 'No backup location configured.*'
- takeScreenshot: restore-flow-b-no-backup-location

# Reconfigure backup location via SAF picker on the restore screen
- tapOn:
    id: 'restore-choose-backup-location-btn'
- waitForAnimationToEnd

- runFlow:
    when:
      visible: 'sdk_gphone64_x86_64'
    commands:
      - tapOn: 'sdk_gphone64_x86_64'
      - waitForAnimationToEnd

- scrollUntilVisible:
    element: 'Pictures'
    direction: DOWN
    timeout: 10000
- runFlow:
    when:
      visible: 'Pictures'
    commands:
      - tapOn: 'Pictures'
      - waitForAnimationToEnd

- tapOn: 'USE THIS FOLDER'
- waitForAnimationToEnd
- tapOn: 'ALLOW'
- waitForAnimationToEnd
- takeScreenshot: restore-flow-b-restore-location-configured

# Dismiss LogBox if present
- runFlow:
    when:
      visible: 'Open debugger to view warnings.'
    commands:
      - tapOn:
          point: '93%,92%'
      - waitForAnimationToEnd

# Wait for backup files to load
- extendedWaitUntil:
    visible: 'Backup files'
    timeout: 10000

# The backup file list should show the backup we just created.
# Use the specific regex computed before the backup so we match the
# right file and not an old one from previous test runs.
- scrollUntilVisible:
    element: ${output.backupFileRegex}
    direction: DOWN
    timeout: 30000
- assertVisible: ${output.backupFileRegex}
- takeScreenshot: restore-flow-b-files-listed

# Select the backup file by tapping the filename text directly.
# The radio circle (leftOf) is too small and unreliable — tapping the
# text label triggers the RadioButton.Item's TouchableRipple, which
# selects the file. This is the same approach proven in Flow A.
- tapOn:
    text: ${output.backupFileRegex}
- waitForAnimationToEnd

# Set a new target DB name (no existing DB to overwrite).
# Scroll up to the input fields at the top of the form.
- scrollUntilVisible:
    element:
      id: 'restore-db-name-input'
    direction: UP
    timeout: 30000
- tapOn:
    id: 'restore-db-name-input'
    point: '95%,50%'
- eraseText
- evalScript: ${output.restoredDbName = "restored_cancel_" + Date.now() + ".db"}
- inputText: ${output.restoredDbName}

# Dismiss the keyboard by tapping a non-interactive area.
- tapOn: 'Backup files'
- waitForAnimationToEnd

- takeScreenshot: restore-flow-b-restore-form-filled

# Dismiss LogBox if present
- runFlow:
    when:
      visible: 'Open debugger to view warnings.'
    commands:
      - tapOn:
          point: '93%,92%'
      - waitForAnimationToEnd

# Scroll down to the Restore button before tapping it.
# The backup file list can be long (accumulated files from prior test
# runs), so use a generous timeout and faster scroll speed.
- scrollUntilVisible:
    element:
      id: 'restore-confirm-btn'
    direction: DOWN
    timeout: 30000
    speed: 80

# Tap Restore — a confirm dialog may appear if the target DB name
# collides with an existing file. Handle it by confirming the dialog.
- tapOn:
    id: 'restore-confirm-btn'
- waitForAnimationToEnd

# If a confirm dialog appeared, tap "Restore" in the dialog to proceed.
- runFlow:
    when:
      visible:
        id: 'restore-dialog-confirm'
    commands:
      - tapOn:
          id: 'restore-dialog-confirm'
      - waitForAnimationToEnd

# Wait for the restore to complete. After a successful restore, the app
# returns to the setup screen (onSuccess sets restoreMode=false).
- extendedWaitUntil:
    visible:
      id: 'db-name-input'
    timeout: 60000
- assertVisible:
    id: 'db-name-input'
- takeScreenshot: restore-flow-b-restore-complete

# ===== OPEN THE RESTORED DATABASE =====

# The setup screen is now showing. Enter the DB name and encryption key
# to open the restored database and verify it works.
- tapOn:
    id: 'db-name-input'
    point: '95%,50%'
- eraseText
- inputText: ${output.restoredDbName}
- tapOn:
    id: 'db-key-input'
- inputText: ${output.dbKey}
- tapOn:
    id: 'db-open-btn'

# Wait for the journal screen to appear — this proves the restored
# database was opened successfully with the correct encryption key.
- extendedWaitUntil:
    visible: 'Create entry'
    timeout: 30000
- assertVisible: 'Create entry'
- takeScreenshot: restore-flow-b-restored-db-opened

# Dismiss LogBox if present
- runFlow:
    when:
      visible: 'Open debugger to view warnings.'
    commands:
      - tapOn:
          point: '93%,92%'
      - waitForAnimationToEnd

# Navigate to Settings and verify the restored database
- tapOn:
    point: '75%,96%'
- waitForAnimationToEnd
- takeScreenshot: restore-flow-b-settings-verified

# Verify database section shows encryption is enabled
- assertVisible: 'Encryption: Enabled'
